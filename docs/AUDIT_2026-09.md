# Logic-flow, UI and UX audit — September 2026

Audited at commit `c61cee4`. Method: three code-reading passes (session engine; screens and view-models; storage, sync and analytics), my own read of routing, session, Today, Body Check and the gate, and a headless run of the real app seeded with a three-day week, captured as an annotated walkthrough (27 screens, 62 pins). Baseline `npm test` was green (1114 + 189 + 53 assertions).

Severity: **H** high, **M** medium, **L** low. The **Batch** column says which pull request fixes the item; a dash means accepted as-is, with the reason.

## Status — 2026-09-14

Since this audit the engine moved into the shared `core/` (paths below read `engine.js` for what is now `core/engine.js`, and so on; `data.js` is still the app's own).

| Items | State | Where |
|---|---|---|
| 1.1 – 1.10 | **fixed** | Batch 1, commit 1863f63 |
| 2.1, 2.2, 2.3, 2.4, 2.7 (gate), 2.10 | **fixed** | the session-safety commit: announcement cut by a tap, STOP asks why, double-tap guard, Skip Rest announces, `rRetryCheck` gated, slow-voice harness + `core/test/session.mjs` |
| 3.3, 3.4, 3.5 (focus, value and tab-strip scroll survive a render), 3.7, 3.10 | **fixed** | the Zone-honesty commit |
| 2.5, 2.6, 2.7 (dead end), 2.8, 2.9 | open | |
| 3.1 (Escape/Enter), 3.2, 3.6, 3.8, 3.9, 3.11, 3.12 | open | 3.2 is the design tokens, deliberately untouched |
| 4.1 – 4.4 | open | 4.1 partly: WebP photos with PNG fallback and lazy loading in the swim app |

Decisions taken with the owner before any fix: report first, then four batches as separate PRs; the red STOP gains an exit that keeps XP; a finished weekday Recovery gets its own week-strip state and Sunday counts toward "this week"; kid-facing screens drop parent wording; contrast is fixed by darkening fills to the existing `*-deep` tokens; iPad landscape is the primary device; the move announcement says the reps or time; the session's exercise list is tidied and Tips & Safety moves.

## 1. XP, prizes and sync (Batch 1)

| # | Sev | Finding | Where |
|---|---|---|---|
| 1.1 | H | `reconcileJourneyWithSessions` summed raw per-row XP stamps while `rebuildJourneyXp` wrote the day-capped total into the same baseline, so any capped date re-granted the difference on every boot, ratcheted `maxLevelSeen`, and could open a real prize draw. | store.js `reconcileJourneyWithSessions` |
| 1.2 | H | While sync was pending, "Add to my prizes" logged the win, said "enjoy it!" and closed; `addPrize` had refused and the wallet was unchanged. `drawIsWaitingOnSync` existed and was called by nobody. | overlays.js `claimPrize`, main.js |
| 1.3 | M | Journey and readiness cloud docs were written whole without re-reading; a device that published before merging erased the other device's prizes and checks in the cloud. | sync.js `flushJourney`, `publishReadiness` |
| 1.4 | M | Day-progress survives 6 h past midnight but the XP budget keyed on the calendar date: one bout, two 360 XP caps. | store.js `dayXpKey`; engine.js `startSession` |
| 1.5 | M | Restore onto a device with any quiz data dropped the backup's quiz mastery; the next rebuild deleted the XP it justified. Readiness log, and the day budget were fill-if-empty only. The athlete registry was not in the backup. | store.js `importProfileData`, `exportProfileData` |
| 1.6 | L | `_syncFailed` started false on every load, so a draw could be claimed in the window before the boot restore answered. | store.js `xpIsPending` |
| 1.7 | L | The amnesty relabel ran on every boot against a prize still in the live pool, so a fresh "Stay up 20 min later" draw was rewritten next launch. | store.js `migratePrizeAmnesty` |
| 1.8 | L | `patchSession` fell back to the last row when its key was not found. | store.js |
| 1.9 | L | Engagement pick keyed a Saturday choice to next week. | store.js `setEngagementPick` |
| 1.10 | L | `saveJourney` ignored its write result. (The banner already fires from `writeStorage`; the return is now propagated.) | store.js |
| — | L | PR-board week pins at 4 after 28 days. Accepted: the board is a four-week block by design. | store.js `getCurrentTrackerWeek` |

## 2. Session flow (Batch 2)

| # | Sev | Finding | Where |
|---|---|---|---|
| 2.1 | H | The phase is set before the coach speaks the move name, so a Done tap during the 3–4 s announcement is discarded as stale by the countdown that starts afterwards; during a reps announcement the tap does nothing at all. The same ordering charges speech time as work (`actualSecs` inflated; an instant Done can record `partial` instead of `skipped`). | engine.js ~1438–1449, `countdown`, `syncClock` |
| 2.2 | H | Red STOP always sets `painFlag` → 0 XP and no streak day, even for a bathroom break; the grey End pays for work done; the overlay never says so. | engine.js `endFromStop`; screens/session.js `stopOverlay` |
| 2.3 | M | A second Done tap inside the 1200 ms stale-flag window ends the following phase too. | engine.js `advance` |
| 2.4 | M | "Skip Rest" sets `preAnnounced`, muting the next move's spoken name. | engine.js ~1600 |
| 2.5 | M | On a resumed day the round label reads "Round 3 of 2" and the round dots show no active dot on the last round. | vm/session.js `roundLabelText`, `roundDots` |
| 2.6 | M | The readiness log stamps the pre-lock light when the locked light lowered a resume; `wasOverridden` then blames a grown-up. | engine.js `stampReadinessOutcome`, finalize |
| 2.7 | M | "Retry" on the Body Check is ungated and clears `severity` and `grownupOk`, bypassing the severity-3 gate. Removing every body-map mark hides the result card (dead end). | vm/readiness.js `resetBodyCheck`, `showInlineBodyResult` |
| 2.8 | L | `openStopOverlay` has no explore guard; `exitSession` cannot delete keys added by `Object.assign`; duplicate keys in `blankSession`; two `LIGHT_ORDER` exports with opposite order; dead `GATE_REASON` keys so the two safety prompts read "…to continue"; unused imports. | engine.js, gate.js, vm/readiness.js |
| 2.9 | L | Announcement does not include the dose (owner request). | engine.js |
| 2.10 | — | Tests stub speech to instant, so none of 2.1–2.4 can be caught. A timing suite is part of the batch. | test/ |

## 3. UI, UX and accessibility (Batch 3)

| # | Sev | Finding | Where |
|---|---|---|---|
| 3.1 | H | Prize-draw overlay had no exit (fixed in Batch 1 alongside the waiting state). No Escape/Enter handling anywhere. | overlays.js, main.js |
| 3.2 | H | White on `--sun` 1.61:1, on `--mint` 2.17:1, `--text-on-aqua` 2.43:1, `--ink-faint` on white 2.57:1 (Back/Skip labels); 273 text declarations under 14 px. | tokens/colors.css, screens/* |
| 3.3 | H | The kid's Today card shows a 🎧 toggle that is a gated action (PIN prompt); the Body Check result shows four light-override buttons and a grown-up checkbox styled as hers. | vm/today.js, screens/readiness.js |
| 3.4 | M | Gate expiry is lazy: Zone contents stay on screen after 5 min until another render. With history, no PIN and no passkey the gate renders no button. | gate.js, main.js `gateHtml` |
| 3.5 | M | Any background render (weather, cloud restore, redeem-undo timer, passkey enrol) wipes text being typed; every scroller resets on every action; journey rail centres once per load. | main.js `render`, vm/today.js |
| 3.6 | M | No safe-area handling; fixed bottom nav and storage banner; banner covers the nav. `shellWithRail`/readiness `min-height:800px`; body-map images fixed 480 px. | shell.js, index.html, readiness.js |
| 3.7 | M | A finished weekday Recovery shows as "CATCH UP" while paying 90 XP and freezing the streak; "this week" can never read 7/7; Sunday inflates adherence. | vm/today.js `weekStatuses`, vm/grownup.js |
| 3.8 | M | Kid's Progress shows `ENDED EARLY`, `NOTHING LOGGED`, `SAFETY STOP`, `MINI`, `TRY-IT` and analytics table rows; severity 3 has three names; four names for Explore; `parentWatch` under a kid heading; Settings addresses kid and parent in adjacent lines; "Valgus gate", "ACWR" unglossed. | vm/progress.js, data.js, screens/* |
| 3.9 | M | Session left panel: 20 px ⓘ per row, long names wrap, list scrolls inside a card; Tips & Safety card competes with the move (owner request to tidy and relocate). | screens/session.js |
| 3.10 | M | ACWR compares an 8-day acute window to 29/4 chronic; gate 14 d vs copy "~4 weeks"; includes recovery/safety-stop minutes; load-trend buckets parsed in device-local time. | vm/grownup.js |
| 3.11 | L | Timer ring and journey card are `<div data-action>` (not focusable); no dialog roles; tabs select by colour only; Done vs Partly-done share a glyph; sub-44 px targets (Redeem, tabs, month arrows, ladder rungs, prize-pool ✕); `exportCsv` and "Save week" give no feedback; sync failure invisible; level-up without a draw never announced. | shell.js, screens/* |
| 3.12 | L | Phone: photo + ring stack under 426 px so Done sits at the fold; detail overlay photo band fixed 330 px. Secondary (iPad first). | screens/session.js |

## 4. Content, photos, dead code (Batch 4)

| # | Sev | Finding | Where |
|---|---|---|---|
| 4.1 | H | 0 of 57 "Demo Image" files exist; Move Library has no fallback and is imageless. 19 Timer images missing (incl. Chair High-Elbow Catch, Pallof Press). Orphans: `Leg Swings -Foam Roller`, `Swimming start.png`, a 1-byte file `new`. | assets/exercises, screens/grownup.js |
| 4.2 | L | `Streamline Hold` dose "2×20s" drives one 40 s clock; coordination "10m" doses drive 60 s clocks; Sunday en-dash ranges parse to the low end; `recoveryHolds` never quizzed or in the library; Mon/Wed have empty `prepMenu`. | data.js, util.js |
| 4.3 | L | `STANDING_RULES` valgus text is the retired rule; main.js comment says "+10" (is 5); date line reads "Week 7" forever from a paused overload anchor. | data.js, main.js, vm/today.js |
| 4.4 | L | Dead: `swim_pr_log`, `swim_learning_records`, `swimTrainingProgressV2`, `swimTrainingSkipHistoryV2`, `timeLo/timeHi`, `HARD_EXERCISES` names, several store/util exports; two of the three "expendable" quota keys are already dead. | store.js, data.js, engine.js |

## Kept as-is (working well)

Deny-by-default action dispatch with one authorization point; every stored string rendered as text; reduced-motion handled; a global focus-visible rule; nine honest completion states; quiz XP honesty copy; the versioned service worker; storage-full surfaced in three places; the visual SET/SIDE/REP chips that let a muted device follow the session.
